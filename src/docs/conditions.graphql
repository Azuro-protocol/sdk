query Conditions(
  $where: Condition_filter!
) {
  conditions(
    where: $where
  ) {
    id
    conditionId
    status
    outcomes {
      outcomeId
      odds: currentOdds
    }
    core {
      address
      liquidityPool {
        address
      }
    }
  }
}
